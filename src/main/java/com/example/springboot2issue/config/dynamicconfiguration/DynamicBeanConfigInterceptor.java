package com.example.springboot2issue.config.dynamicconfiguration;

/**
 * Base interceptor which must be implemented by all dynamic bean config interceptors to intercept method calls and delegate to specific methods.
 */
public interface DynamicBeanConfigInterceptor {
}
